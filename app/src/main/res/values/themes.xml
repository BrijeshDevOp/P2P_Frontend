<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WebRTCDataChannelTY" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Remove custom background; use system default surface colors -->
        <!-- System navigation bar -->
        <item name="android:navigationBarColor">@color/brand_primary</item>
        <!-- Default button styles -->
        <item name="buttonStyle">@style/AppButton</item>
        <item name="materialButtonStyle">@style/AppMaterialButton</item>
        <!-- App logo for light theme -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/app_logo_light</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.WebRTCDataChannelTY.Splash" parent="Theme.WebRTCDataChannelTY">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>